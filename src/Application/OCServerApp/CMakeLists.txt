INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})
INCLUDE_DIRECTORIES(${WORKINGPATH}/OsgCesium/Core)
INCLUDE_DIRECTORIES(${WORKINGPATH}/OsgCesium/Core/OCThread)
INCLUDE_DIRECTORIES(${WORKINGPATH}/3rdParty/x64/include)
INCLUDE_DIRECTORIES(${WORKINGPATH}/OpenSceneGraph/include)
INCLUDE_DIRECTORIES(${WORKINGPATH}/OSGEarth/src)

SET(LIB_NAME OCServerApp)

SET(TARGET_H

)

SET(TARGET_SRC
    OCServer.rc
    main.cpp
)

SET(TARGET_COMMON_LIBRARIES 
    OpenThreads
    osg
    osgDB
    osgEarth
    OCUtility
    OCHttpServer
)

SET(TARGET_EXTERNAL_LIBRARIES 
    zlib
)

SETUP_APPLICATION(${LIB_NAME})
